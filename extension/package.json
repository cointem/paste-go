{
  "name": "paste-go",
  "displayName": "Paste Go",
  "description": "Smart Paste for VS Code with AI Fallback",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.80.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "paste-go.smartPaste",
        "title": "Smart Paste (Struct)",
        "category": "Paste Go"
      }
    ],
    "keybindings": [
      {
        "command": "paste-go.smartPaste",
        "key": "ctrl+alt+v",
        "mac": "cmd+alt+v",
        "when": "editorTextFocus"
      }
    ],
    "configuration": {
      "title": "Paste Go",
      "properties": {
        "pasteGo.corePath": {
          "type": "string",
          "default": "",
          "description": "Path to the Paste Go Go binary. If empty, tries to find in extension folder."
        },
        "pasteGo.aiApiKey": {
          "type": "string",
          "default": "",
          "description": "API Key for AI Fallback."
        },
        "pasteGo.aiProvider": {
          "type": "string",
          "default": "gemini",
          "description": "AI Provider to use for fallback (e.g., gemini, openai, deepseek, moonshot).",
          "enum": [
            "gemini",
            "openai",
            "deepseek",
            "moonshot"
          ]
        },
        "pasteGo.aiModel": {
          "type": "string",
          "default": "",
          "description": "Specific model name (e.g., 'gpt-4o', 'gemini-1.5-pro'). Leave empty for default."
        },
        "pasteGo.aiBaseUrl": {
          "type": "string",
          "default": "",
          "description": "Base URL for OpenAI-compatible providers (e.g. DeepSeek)."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "tsc -b",
    "watch": "tsc -b -w",
    "package": "vsce package",
    "test": "vitest"
  },
  "devDependencies": {
    "@types/vscode": "^1.80.0",
    "@types/node": "^18.0.0",
    "typescript": "^5.0.0",
    "ts-loader": "^9.4.0",
    "webpack": "^5.0.0",
    "webpack-cli": "^5.0.0",
    "vsce": "^2.15.0"
  }
}
